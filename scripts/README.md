# Scripts

品牌配置工具链说明见 [config-tool/README.md](./config-tool/README.md)。

```bash
pnpm run config:validate -- <config-dir> --channel <channel>
pnpm run config:apply -- <config-dir> --channel <channel>
pnpm run brand:verify -- --channel <channel>
```
